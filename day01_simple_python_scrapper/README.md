# A very tiny and simple web scrapper in Python

## Installation

In case you need to install, I used these libraries:

```
$ sudo pip install BeautifulSoup
$ sudo pip install Requests

```

## Usage

Run:

```
$ python hn_scrapper.py
